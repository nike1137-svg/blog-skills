# 블로그 스킬 전문 (합본)

> **이 파일은 자동 생성됩니다. 직접 고치지 마세요.**
> 원본은 `.claude/skills/<이름>/SKILL.md` 이고,
> 고친 뒤 `bash tools/build-all.sh` 를 돌리면 이 파일이 갱신됩니다.

생성: 2026-08-31 19:18 · 스킬 10개

## AI 에게

블로그 글 작업이면 아래 스킬들을 **전부 읽고** 그 규칙대로 진행합니다.
순서는 `draft-to-post` 파이프라인을 따릅니다.
`lms-safe` 는 LMS 강의 내용을 옮길 때 **가장 먼저** 적용합니다.
기업 연계 과제 내용은 **대외비** 이므로 회사명·실명·분야 용어·코드를
적지 않고 방법론만 남깁니다.

## 목차

- [`analogy-maker`](#analogy-maker)
- [`blog-retro`](#blog-retro)
- [`concept-bridge`](#concept-bridge)
- [`draft-to-post`](#draft-to-post)
- [`jekyll-frontmatter`](#jekyll-frontmatter)
- [`lms-safe`](#lms-safe)
- [`my-role`](#my-role)
- [`readme-standard-check`](#readme-standard-check)
- [`term-card`](#term-card)
- [`tone-keeper`](#tone-keeper)


---

<a id="analogy-maker"></a>

# 스킬: analogy-maker

---
name: analogy-maker
description: 이해가 실제로 막히는 기술 개념에만 절제해서 비유를 붙일 때 사용. 쉽게 풀어주려는 게 아니라, 정확한 용어를 그대로 두고 이해를 돕는 보조로만 쓴다.
---

# 이해 보조 비유 생성 (analogy-maker)

> **전제(2026-08-26 갱신): 비유는 기본값이 아니라 예외다.**
> 글쓴이는 이제 대부분의 용어를 정확히 이해하고 쓴다. 비유의 목적은
> "비전문가도 읽히게 쉽게 풀기"가 **아니라**, **이해가 실제로 걸리는 지점에서만**
> 그 걸림을 풀어주는 것이다. 용어는 그대로 쓰고, 비유는 보조로만 얹는다.

기술 개념 하나를 받아, 정말 필요할 때 **이해를 돕는 비유 한 줄**을 만든다.

## 먼저 — 비유를 붙일지부터 판단한다
아래에 하나라도 해당하면 **비유를 붙이지 않는다.** 용어를 그대로 쓴다.
- 글쓴이가 이미 다른 글에서 쓰는 어휘다 → [[tone-keeper]]의 「이미 익숙한 어휘」 참고.
- 용어의 이름만으로 동작이 대충 짐작되는 것(예: 자동 시작, 재시도, 로그).
- 그 글에서 이미 한 번 비유를 쓴 개념 — 반복해서 비유하지 않는다.

비유는 **처음 나오는, 동작이 직관에 어긋나거나 오해하기 쉬운 개념**에만 붙인다.
한 글에 비유가 두세 개를 넘으면 과하다. 없는 게 맞는 글도 많다.

## 붙이기로 했다면 — 좋은 비유의 조건
- **손에 잡히는 사물/행동**에 빗댄다: 계산대 옆 바구니, 창고, 책 뒤 색인, 레시피, 문과 자물쇠, 상자.
- 개념의 **핵심 동작 한 가지**만 정확히 대응시킨다(너무 많이 갖다 붙이지 않는다).
- 비유가 **어디서 깨지는지**도 한 줄로 덧붙이면 더 좋다(정직한 서술).
- 추상 비유("흐름처럼", "마법같이") 금지. 전문 용어로 전문 용어를 설명하지 않는다.
- **용어를 대체하지 않는다.** 정확한 용어를 먼저 쓰고, 비유는 그 옆에 보탠다.

## 블로그 실제 비유 (톤 레퍼런스)
- 캐시 → "자주 찾는 물건을 **계산대 옆 바구니**에 올려 둠"
- 색인(인덱스) → "책 뒤 **색인**에서 단어 찾듯, 처음부터 안 넘겨도 됨"
- SQL 인젝션 → "레시피에 몰래 한 줄 끼워 넣어 엉뚱한 요리를 시키는 것"
- 배포 터널 → "안에서 밖으로 낸 **터널**, 문은 나만 들어가게 잠금"

## 출력 형식
개념마다:
```
- **비유**: (일상 사물/행동에 빗댄 한 문장)
- (선택) **어디서 깨지나**: (비유의 한계 한 줄)
```
필요하면 후보 2~3개를 주고 고르게 한다.


---

<a id="blog-retro"></a>

# 스킬: blog-retro

---
name: blog-retro
description: 강의·과제·작업 노트(날것)를 조경호의 '학습 일지' 회고 글로 변환할 때 사용. 그날 핵심을 담은 제목(은유는 선택)·전날과 이어지는 도입부·번호 섹션(개념→구현→검증→보안→회고)·내 역할 마무리·다음 예고까지 한 번에 완성한다.
---

# 학습 일지 회고 글 변환 (blog-retro)

> ⚠️ **먼저 [[lms-safe]]를 적용한다.** 입력이 LMS/강의 노드 노트면, 회고 글로 옮기기 전에 lms-safe 가드(원문 복붙·6어절 이상 연속 동일·코드 그대로·조기 공개 금지, 자체 소화 재작성)를 반드시 통과시킨다.

날것의 노트를 받아 조경호 블로그(GitHub Pages / Chirpy)의 회고 일지 한 편으로 완성한다.
결과물은 그대로 `_posts/`에 넣어 발행할 수 있어야 한다.

## 입력으로 받는 것
- 그날 배운 것 / 한 작업 메모 (순서·형식 무관, 거칠어도 됨)
- (있으면) 전날 글의 마지막 "다음 예고" 문장 → 오늘 도입부로 이어받는다

## 만드는 순서

1. **제목의 축을 정한다.** 그날 작업에서 가장 중요한 발견·반전 하나를 제목으로 세운다.
   - 은유형 제목은 **선택지의 하나일 뿐 의무가 아니다.** 은유가 그날의 핵심을 더 정확히
     드러낼 때만 쓴다. 그렇지 않으면 **발견 자체를 담은 담백한 제목**이 낫다.
     예: "파인튜닝이 1등이 아니었다", "copy baseline을 안 쟀으면 반대로 결론 낼 뻔했다".
   - 은유를 쓰기로 했다면 제목·도입부에서 자연스럽게 이어지되, 억지로 섹션마다
     끌고 다니지 않는다. 내용이 앞서고 은유는 거들 뿐이다.

2. **Front matter를 만든다.** → 세부 규칙은 [[jekyll-frontmatter]] 스킬을 따른다.
   - `title: "제목 — 한 줄 부제"` (제목은 은유형/담백형 중 그날 핵심을 더 잘 드러내는 쪽)
   - `categories:` 개념 정리 위주면 `[일지, 개발]`, 프로젝트 회고면 `[프로젝트]`
   - `description:` 전날 예고를 이어받아 "무엇을 한 날인지" 1~3문장

3. **도입부(1문단).** 전날 글의 예고를 받아 오늘 작업으로 잇는다.
   전날 예고가 없으면 그날의 문제의식으로 담백하게 연다.

4. **번호 섹션을 쌓는다.** `## 1.` `## 2.` … 순서로, 대체로 이 흐름:
   - 개념(무엇을·왜) → 구현(실제로 한 것) → 검증(확인한 방법) → 보안/주의 → 회고
   - **용어는 그대로 쓴다.** 이해가 실제로 걸리는 개념에만 비유를 절제해서 붙인다 → [[analogy-maker]].
     대부분의 용어는 비유 없이 그대로 두는 게 맞다.
   - 코드/명령은 인라인으로 두고 **화살표(→) 뒤에 한국어 설명**을 붙인다.

5. **로드맵 표(선택).** 프로젝트 시리즈 글이면 Phase 0~7 표에서 오늘 완료 단계를 표시.

5-1. **과거 글과 잇는다.** → [[concept-bridge]]
   `_posts/`를 **실제로 grep해서**, 오늘 결론과 이어지는 예전 글이 있는지 찾는다.
   있으면 그때 문장을 **인용해서** 잇는다(링크만 걸지 않는다. 인용은 그 자리에서 읽힌다).
   글이 쌓이면 각 글은 그날 안에서 닫혀 연결이 안 보인다. 찾는 일을 기억에 맡기지 않는다.
   찾아본 결과 없으면 생략하되, **안 찾아보고 없다고 하지 않는다.**

6. **마무리.** "AI가 한 일 vs 내가 판단한 일" 정리로 닫고 → [[my-role]],
   마지막 줄에 **다음 예고 한 문장**을 남긴다(다음 글 도입부가 될 씨앗).
   - ⚠️ **다음 예고는 지어내지 않는다. 글쓴이에게 물어서 실제 계획을 그대로 적는다.**
     이 블로그는 매 글이 앞 글의 예고를 인용하며 시작하므로, 만들어 넣은 예고는
     다음 글에서 정정해야 하는 빚이 된다. 물어볼 것과 답을 못 받았을 때의
     처리는 [[my-role]]에 있다.

7. **말투 점검.** 전체를 조경호 특유의 톤으로 다듬는다 → [[tone-keeper]].

## 지켜야 할 톤
- 대화체·성찰형. "알고 가기", "실수도 했다" 같은 솔직한 자기 서술을 허용.
- 승리 자랑보다 **과정과 이해**를 강조(속도보다 이해).
- **정확한 용어가 먼저다.** 글쓴이는 용어를 알고 쓰므로 그대로 쓴다.
  비유는 이해가 실제로 걸리는 곳에만 보조로 얹는다(쉽게 풀기가 목적이 아니다).

## 출력
완성된 마크다운 전문(front matter 포함)과, 저장할 파일명 `YYYY-MM-DD-slug.md`를 함께 제시한다.

**제시까지가 이 스킬의 범위다.** 글쓴이가 전문을 읽고 발행에 동의하기 전에는
커밋·푸시하지 않는다. 카테고리·제목 같은 개별 항목에 답이 왔다고 해서
전문을 승인한 것이 아니다. 발행 절차는 [[draft-to-post]]의 「발행 단계」를 따른다.


---

<a id="concept-bridge"></a>

# 스킬: concept-bridge

---
name: concept-bridge
description: 회고·실습 글을 쓰거나 손볼 때 과거 글과의 연결을 찾아 잇는다. 배운 개념은 "관련 개념" 링크 박스로, 과거에 내린 결론은 본문에서 인용해 잇는다. 글이 쌓이면 각 글이 그날 안에서 닫혀 연결이 안 보이므로, 기억이 아니라 저장소를 실제로 뒤져 확인한다. 새 글을 마무리할 때(draft-to-post 파이프라인 안), 또는 기존 글에 소급 적용할 때 쓴다.
---

# 과거 글과 잇기 (concept-bridge)

목적이 둘이다.

1. **"그때 배운 개념이 여기서 이렇게 쓰였다"** — 개념과 실습의 다리
2. **"그때 내가 내린 결론이 오늘 작업과 이어진다"** — 결론과 결론의 다리

두 번째는 2026-08-31에 추가됐다. 그날 실제로 이런 일이 있었다. 나흘 전 글에
**"파인튜닝과 RAG가 보완재"**라고 써 두고도, 그날 한 메인퀘스트가 바로 그 문장의
오른쪽 절반이라는 걸 못 보고 글을 끝낼 뻔했다. 글쓴이가 먼저 물어서야 드러났다.

글이 쌓이면 **각 글은 그날 안에서 닫힌다.** 나흘 전 결론이 오늘 작업과 이어지는지는
누가 찾아보지 않으면 보이지 않는다. 이건 글쓴이가 이해를 못 해서가 아니라 글이
흩어져 있어서 생기는 일이므로, **찾는 일을 사람 기억에 맡기지 않는다.**

## 언제 쓰나

**회고·실습 성격의 글이면 카테고리를 가리지 않고 전부 거친다.**

`[일지, 실습]` · `[일지, 개념]` · `[일지, 회고]` ·
`[프로젝트, 개인프로젝트]` · `[프로젝트, 메인퀘스트]` · `[프로젝트, 과제]`

> 2026-08-31 확대. 그전에는 `[일지, 실습]`과 `[프로젝트, 개인프로젝트]` 둘만
> 대상이라 **메인퀘스트 글이 통째로 빠져나갔다.** 위의 누락이 거기서 났다.

용어정리(`[일지, 용어정리]`)와 일상(`[일지, 일상]`)은 대상이 아니다.
이미 발행된 글에 소급 적용할 때도 같은 규칙을 쓴다.

## 0단계 — 저장소를 실제로 뒤진다 (건너뛰지 않는다)

**기억이나 짐작으로 하지 않는다.** 오늘 글에서 핵심 낱말·수치·결론 문장을 뽑아
각각 찾아본다.

```bash
grep -rl "<핵심어>" _posts | sort | tail -20        # 어느 글에 있나
grep -n "<핵심어>" _posts/<후보>.md                  # 그 글에서 무슨 말을 했나
```

뽑을 것의 예 — 그날 다룬 기술 이름(`RAG` `파인튜닝` `프롬프트` `임계값`),
그날 나온 수치(`20.7초` `6.5배`), 프로젝트 이름(`온담이`), 그날의 결론 낱말.

- 후보가 나오면 **그 대목을 열어 실제로 무슨 말을 했는지 읽는다.** 제목만 보고 판단하지 않는다.
- 오늘 결론과 **반대되는** 말을 과거에 했다면 그것도 연결이다. 오히려 더 좋은 연결이다.

## (가) 관련 개념 — 링크 박스

1. 글 본문에서 실제로 적용된 개념을 뽑는다 (트랜잭션, Docker 컨테이너, IPv6, RLS 등).
   지어내지 말고 본문에 실제로 등장하는 것만.
2. `_posts/`의 `[일지, 개념]` 글 중 그 개념을 다루는 글을 찾는다.
3. 매칭되면 **description 아래, 첫 섹션 시작 전**에 짧은 링크 박스를 넣는다.
   ```
   > 관련 개념: [개념 글 제목](/blog/posts/slug/)
   {: .prompt-info }
   ```
   개념이 여럿이면 가운데 점(·)으로 잇는다. 핵심 2~3개로 추리고 4개를 넘지 않는다.
4. 매칭되는 개념 글이 없으면 **억지로 만들지 않고 생략한다.**

## (나) 과거 결론 — 본문에서 인용해 잇기

오늘 글의 결론이 과거 글의 결론과 이어지면, 링크만 걸지 말고 **그때 쓴 문장을
인용해서** 잇는다. 링크는 눌러야 보이지만 인용은 그 자리에서 읽힌다.

들어갈 자리는 대체로 둘이다.

- **본문 중간** — 오늘 잰 값이 과거에 잰 값과 이어지거나 어긋날 때, 그 절 안에서
- **"다음은" 앞** — 오늘 한 일이 과거 결론의 어느 부분이었는지 자리매김할 때

쓰는 모양:

```markdown
나흘 전 글([제목](/blog/posts/slug/))에서 이렇게 적었다.

> (그때 문장 그대로)

(오늘 것이 그 문장과 어떻게 이어지는지 한두 문장)
```

**인용은 원문과 글자를 대조한다.** 기억으로 옮겨 적지 않는다.

```bash
grep -o "인용하려는 문장 앞부분" _posts/<그 글>.md
```

숫자가 비슷해서 같은 것으로 보이지만 실은 다른 것이면, **다르다는 것을 밝히는 게
연결이다.** (실제 예: 20.7초는 답을 쓰는 시간, 19~21초는 질문을 읽는 시간 — 손잡이가 다르다.)

## 체크리스트

- [ ] **0단계 grep을 실제로 돌렸는가** — 안 돌리고 "연결 없음"이라고 하지 않았는가
- [ ] 링크가 실제로 존재하는 글(파일)을 가리키는가 — slug를 지어내지 않았는가
- [ ] 인용문이 **원문과 글자까지 같은가**
- [ ] 관련 개념이 4개를 넘지 않는가
- [ ] Chirpy prompt 박스 문법(`{: .prompt-info }`)이 맞는가
- [ ] 없는 연결을 억지로 만들지 않았는가 — **정말 없으면 없는 게 맞다**

## 하지 않는 것

역방향(개념 글에 "이 개념이 쓰인 실습" 링크)은 기본적으로 하지 않는다.
개념 글 하나가 여러 실습에 걸치면 링크가 계속 늘어나 글이 지저분해진다.
필요하면 글쓴이에게 먼저 확인한다.

## draft-to-post와의 관계

[[jekyll-frontmatter]]로 slug·categories가 확정된 뒤, [[tone-keeper]] 최종 교정 전에 거친다.
**회고·실습 성격이면 카테고리를 가리지 않고 거친다.**


---

<a id="draft-to-post"></a>

# 스킬: draft-to-post

---
name: draft-to-post
description: 거친 노트 한 뭉치를 발행 준비가 끝난 블로그 글 한 편으로 완성할 때 사용. 회고 변환·front matter·비유·내 역할·말투 스킬을 순서대로 엮는 원스톱 파이프라인.
---

# 초안 → 발행 완성 파이프라인 (draft-to-post)

> ⚠️ **0단계: 먼저 [[lms-safe]]를 적용한다.** 입력이 LMS/강의 노트면, 파이프라인을 돌리기 전에 원문 복붙·6어절 이상 연속 동일 표현·코드 그대로 게시·조기 공개를 막는 lms-safe 가드를 반드시 통과시킨다. 게시 전 lms-safe 체크리스트도 통과해야 한다.

노트만 던지면 `_posts/`에 바로 넣을 수 있는 글 한 편을 끝까지 만든다.
개별 스킬을 아래 순서대로 호출·조합하는 **오케스트레이터**다.

## 파이프라인

1. **글 종류 판별**
   - 개념/작업 회고 → 일지 글, 용어 여러 개 → '오늘의 용어 정리' 글.
   - 용어 글이면 [[term-card]]로 넘겨 카드들을 만든 뒤 5번(front matter)으로.

2. **본문 구성** → [[blog-retro]]
   은유 정하기 → 도입부 → 번호 섹션(개념→구현→검증→보안→회고) → (프로젝트면 로드맵 표).

3. **어려운 대목 비유 보강** → [[analogy-maker]]
   전문 용어가 처음 나오는 자리마다 일상 비유 한 줄.

4. **마무리** → [[my-role]]
   "AI가 한 일 vs 내가 판단한 일" 정리 + 다음 예고 한 문장.
   ⚠️ **다음 예고는 글쓴이에게 물어서 받는다. 지어내지 않는다.**
   그럴듯해 보여도 추측해서 쓰면 다음 글이 그 예고를 정정하며 시작해야 한다.

5. **파일명 + Front matter** → [[jekyll-frontmatter]]
   제목·카테고리·태그·날짜·설명 확정, `_posts/YYYY-MM-DD-slug.md` 결정.

6. **과거 글과 잇기** → [[concept-bridge]]
   회고·실습 성격이면 **카테고리를 가리지 않고** 거친다(메인퀘스트·과제 포함).
   `_posts/`를 **실제로 grep해서** 두 가지를 찾는다.
   - 이 글이 적용한 `[일지, 개념]` 글 → "관련 개념" 링크 박스
   - **과거에 내가 내린 결론 중 오늘 것과 이어지는 것** → 본문에서 그때 문장을 인용해 잇는다
   찾아본 결과 정말 없으면 생략한다. 다만 **안 찾아보고 없다고 하지 않는다.**

7. **말투 최종 교정** → [[tone-keeper]]
   과장 제거, 은유 일관성, 마지막 여운 확인.

8. **발행** — ⚠️ **여기서 반드시 멈춘다.**
   아래 「발행 단계」의 조건을 통과하기 전에는 커밋·푸시하지 않는다.

## 발행 단계 (2026-08-31 추가)

> 이 절이 없던 동안, 파이프라인이 산출물 제시에서 끝나고 **커밋·푸시가 절차 밖에**
> 있었다. 그래서 글쓴이가 카테고리 하나에 답한 것을 발행 승인으로 읽고 그대로
> 올린 일이 있었다. 규칙을 문서 어딘가에 적어 두는 것으로는 안 걸린다.
> **파이프라인 단계로 박아 두어야 걸린다.**

**조건** — 산출물(전문)을 제시했다.

**발동** — 아래를 확인하고, 하나라도 아니면 **멈춘다.**

- [ ] 글쓴이가 **전문을 읽었다고 확인**되는가
- [ ] 글쓴이가 **발행에 동의**했는가

**개별 항목에 대한 답은 발행 승인이 아니다.** 카테고리·제목·태그·예고 같은
질문에 답이 왔다고 해서 전문을 승인한 것이 아니다. 그 답들은 초안을 고치라는
지시이고, 고친 뒤에 **다시 보여 주고 승인을 받는다.**

승인이 없으면 커밋·푸시하지 않는다. 초안 파일은 만들어 두되 `git add` 도 하지 않는다.

승인을 받았으면 커밋·푸시하고, **빌드가 끝나 실제 주소가 200 인지까지 확인**한다.
(Jekyll 은 발행 시각이 미래면 빌드에서 빼므로 404 가 날 수 있다.)

## 산출물 (한 번에 제시)
- 저장 경로/파일명: `_posts/YYYY-MM-DD-slug.md`
- front matter 포함 마크다운 **전문**
- 발행 전 셀프 체크:
  - [ ] 전날 예고를 이어받았는가
  - [ ] 어려운 용어마다 비유가 있는가
  - [ ] 마무리에 '내 판단'과 '다음 예고'가 있는가
  - [ ] **다음 예고가 글쓴이에게 물어서 받은 실제 계획인가** — 지어낸 것이면 지운다
  - [ ] categories가 2단계인가, date에 `+0900`이 있는가
  - [ ] **date가 실제 현재 시각보다 과거인가** (미래면 Jekyll이 빌드에서 빼서 404 — 시스템 시계로 확인)
  - [ ] 관련 개념 링크가 실제 존재하는 글을 가리키는가 — 없으면 억지로 달지 않았는가
  - [ ] **과거 글을 실제로 grep해봤는가** — 오늘 결론과 이어지는 예전 결론이 있는지.
        인용했다면 원문과 글자까지 대조했는가
  - [ ] **글쓴이가 전문을 읽고 발행에 동의했는가** — 항목별 답변은 동의가 아니다.
        동의 전에는 `git add` 도 하지 않는다

## 원칙
확정이 애매한 값(제목 은유·카테고리·slug)은 지어내지 말고 후보를 제시해 고르게 한다.


---

<a id="jekyll-frontmatter"></a>

# 스킬: jekyll-frontmatter

---
name: jekyll-frontmatter
description: 조경호 블로그(GitHub Pages / Chirpy) 새 글의 파일명과 YAML front matter를 규칙대로 자동 생성할 때 사용. 제목·카테고리·태그·날짜·설명 필드를 형식에 맞춰 만든다.
---

# Jekyll Front matter 생성 (jekyll-frontmatter)

Chirpy 테마 규칙에 맞춰 새 글의 **파일명 + front matter**를 만든다.
값을 지어내기보다 입력에서 뽑아내되, 빠진 필드는 규칙대로 채운다.

## 파일명
```
_posts/YYYY-MM-DD-slug.md
```
- 날짜는 발행일(KST 기준).

## ⚠️ 발행 시각 규칙 (필수 — 안 지키면 404)
**Jekyll은 발행 시각이 "미래"인 글을 빌드에서 제외한다 → 사이트에서 404.**
그래서 `date`는 다음을 반드시 지킨다.
1. 시각을 **지어내지 않는다.** 글을 쓰는 시점의 **실제 현재 시각**을 확인한다.
   - 윈도우: `Get-Date -Format "yyyy-MM-dd HH:mm:ss zzz"`
   - 리눅스/맥: `date "+%Y-%m-%d %H:%M:%S %z"`
2. 확인한 값보다 **살짝 과거(예: 몇 분 전)** 로 잡아 미래-글 제외를 확실히 피한다.
3. 배포(Actions) 지연을 감안해, 애매하면 **더 과거로** 잡는다. (과거는 안전, 미래는 404)
- `slug`은 영어 kebab-case, 소문자. (예: `server-remembers`, `terms-database`)
- 용어정리 글은 `terms-주제`, 프로젝트 글은 내용 기반 slug.

## Front matter 형식
```yaml
---
title: "은유형 제목 — 한 줄 부제"
date: YYYY-MM-DD HH:MM:SS +0900
categories: [상위, 하위]
tags: [태그1, 태그2, 태그3]
description: 글 미리보기 1~3문장. 무엇을 한/배운 날인지, 가능하면 전날 글과 잇는 한마디.
---
```

## 필드 규칙
- **title**: `"핵심 은유 — 부제"` 형태. em대시 `—`로 제목과 부제를 나눈다.
  - 예: `"마지막 문을 연 날 — 터널로 세상과 잇고, 나만 들어가게"`
- **date**: 반드시 `+0900`(KST) 포함. 시:분:초까지. **실제 현재 시각을 확인해 과거로 넣는다**(미래면 404 — 위 '발행 시각 규칙' 참고).
- **categories**: 항상 **2단계** `[상위, 하위]`. (2026-07-24 재구조화)
  - 배움/개념 정리(직접 만든 결과물보단 이해 위주) → `[일지, 개념]`
  - 직접 돌려서 결과를 관찰·측정한 실습(코랩 실험·챗봇 배포·양자화 실측 등, 과제·메인퀘스트 아닌 것) → `[일지, 실습]`
  - 용어 모음 → `[일지, 용어정리]`
  - **도구를 설치·세팅한 것 자체가 중심**인 글(순수 설치기) → `[일지, 도구]`. (돌려보고 관찰이 중심이면 도구가 아니라 실습)
  - 프로젝트/블로그 운영 전체를 되짚는 회고(내 판단 중심) → `[일지, 회고]`
  - 기술과 무관한 그날의 감상·다짐 → `[일지, 일상]`
  - 스스로 만든 사이드 프로젝트(예: 새록이) → `[프로젝트, 개인프로젝트]`
  - 교육기관 실습 과제(예: 병원동행) → `[프로젝트, 과제]`
  - 교육기관 메인퀘스트(예: MedCheck) → `[프로젝트, 메인퀘스트]`
  - 애매하면 지어내지 말고 후보를 제시해 고르게 한다.
- **tags**: 한글·영어 혼합 허용. 그 글에 등장한 기술/키워드를 그대로.
  자주 쓰는 것: `배포, 보안, 회고, 로드맵, 데이터베이스, Docker, 백엔드`.
- **description**: 검색·목록에 노출되는 미리보기. 전날 예고를 이어받으면 시리즈 느낌이 산다.

## 출력
파일명 한 줄 + `---`로 감싼 front matter 블록. 애매한 필드는 후보를 제시하고 확인을 받는다.


---

<a id="lms-safe"></a>

# 스킬: lms-safe

---
name: lms-safe
description: LMS(모두의연구소 노드/강의) 학습 내용을 블로그로 옮길 때 저작권·게시 정책을 자동으로 지키게 하는 필수 가드. 원문 복붙·6어절 이상 연속 동일 표현·코드 그대로 게시·조기 공개를 막고 '자체 소화'로 재작성하게 한다. LMS·강의·수업·노드 노트를 블로그 글로 만들 때 term-card·blog-retro·draft-to-post보다 먼저 적용한다.
---

# LMS 저작권 준수 가드 (lms-safe)

목적: **LMS 노드(강의) 내용을 블로그로 옮길 때** 모두의연구소 저작권/게시 정책을 자동으로 지킨다.
블로그 글 생성 스킬([[term-card]] · [[blog-retro]] · [[draft-to-post]])을 쓰기 **전에, 항상 먼저** 적용한다.

## 왜 (배경)
LMS 노드 콘텐츠는 **복제·제3자 전달 금지**다. **공개 블로그 게시 = 제3자 전달**이라, 정책은 "자체적으로 소화한 내용"만 규칙에 맞게 게시하도록 허용한다. 즉 문제는 '블로그'가 아니라 '복붙'이다. 방식만 지키면 계속 기록해도 된다.

## 절대 규칙 (하나라도 어기면 게시 불가)
1. **원문은 참고만.** LMS 텍스트를 옮겨 쓰지 않는다. 읽고 이해한 뒤 **완전히 내 말로 재작성**.
2. **6어절 / 1문장 이상 연속 동일 표현 금지.** (예외: `MVP`·`RLS`·`HTTP` 같은 **공인된 전문 용어 자체**는 허용.)
3. **코드·주석 그대로 게시 금지.** 노드가 제공한 코드를 그대로/일부 그대로 나열 금지. **내가 변형·추가한 코드만**, 설명은 내 문장으로.
4. **출처 목록·통계 문장 복붙 금지.** 필요하면 **원 출처를 직접 확인해** 내 문장으로 쓴다.
5. **공개 시점:** 해당 노드의 **제출 기한 + 루브릭 평가 완료 이후에만** 공개. 그 전에는 **비공개 초안**으로만 저장.
6. **정의·비유·예시도 재현 금지.** 노드의 정의/예시를 베끼지 말고 내가 이해한 대로 새로 쓴다.

## 작성 방법 — '자체 소화'
- 원문을 덮고, **머릿속에서 핵심만 다시 설명한다**는 마음으로 쓴다.
- 흐름: 개념 → **내가 이해한 것** → **내 경험·적용·판단**. (설명의 재현이 아니라 소화의 결과)
- 비유·예시는 **새로** 만든다(원문 비유 재사용 금지) → [[analogy-maker]].
- 강한 글은 원문 요약이 아니라 **"나는 이걸 이렇게 이해했고, 어디에 쓸 것 같다"** 다.

## 게시 전 자체 점검 (필수 체크리스트)
커밋/공개 전에 반드시 확인하고, 하나라도 걸리면 고친다:
- [ ] 원문과 **6어절 이상 연속 겹치는 문장**이 없는가? (의심되면 그 문장 재작성)
- [ ] 노드 **코드·주석을 그대로** 넣지 않았는가?
- [ ] **출처·수치 문장을 복붙**하지 않았는가?
- [ ] (공개라면) **제출 기한 + 루브릭 평가 완료 이후**인가? 아니면 **비공개 유지**.
- [ ] 전체가 **"내가 소화한 내용"**으로 읽히는가? (원문 요약처럼 읽히면 다시 쓴다)

## 애매하면
- 공개하지 말고 **비공개 초안**으로 두고, 사용자에게 알린다.
- 정책 해석이 불확실하면 **운영진 확인**을 권한다. (이 스킬은 정책 문서 기준의 가이드이지 공식 유권해석이 아니다.)

## 출력
- 재작성된 글(자체 소화본) + 위 **체크리스트 통과 여부** 표시.
- 공개 시점 미충족이면 **"비공개 초안"** 으로 권고.


---

<a id="my-role"></a>

# 스킬: my-role

---
name: my-role
description: 회고 글 마무리로 'AI가 한 일 vs 내가 판단한 일'을 나누는 책임 정리를 만들 때 사용. 도구에 권한은 주되 검토 책임은 내가 진다는 관점을 드러낸다.
---

# 내 역할 마무리 (my-role)

글 끝에서 그날의 작업을 **"AI가 한 일 / 내가 판단·검토한 일"**로 나눠 정리한다.
조경호 블로그의 핵심 관점 — "AI가 코드를 쓰고, 나는 어떤 모양의 데이터로·무엇을 택할지 판단한다" — 를 매 글 끝에 심는다.

## 왜 이걸 넣나
- 도구에 실행을 맡기되 **검토 책임까지 넘기지 않는다**는 태도를 남긴다.
- 나중에 읽는 사람(그리고 미래의 나)에게 "무엇을 이해하고 넘어갔는지" 증거가 된다.
- "손으로 한 일은 적어두지 않으면 남지 않는다"는 원칙의 실천.

## 형식
그날 내용에 맞춰 아래 둘 중 하나로:

**표 형태**
```markdown
| AI가 한 일 | 내가 판단·검토한 일 |
|---|---|
| 코드/설정 초안 작성 | 어떤 방식을 쓸지 선택, 결과 직접 확인 |
```

**체크리스트 형태**
```markdown
### 오늘 내가 판단한 것
- [ ] (정규화/인덱스/트랜잭션 등) 왜 이렇게 정했는지
- [ ] 실행 결과를 내가 직접 확인했는가
- [ ] 보안·개인정보에서 최소한만 남겼는가
```

## 규칙
- **자랑이 아니라 판단의 기록**으로 쓴다. "빠르게 했다"보다 "이래서 이렇게 정했다".
- 실수했으면 실수도 한 줄 남긴다(정직한 톤).
- 마지막엔 자연스럽게 **다음 예고 한 문장**으로 넘어갈 수 있게 여지를 둔다.

## ⚠️ 다음 예고는 지어내지 않는다 (2026-08-31 추가)

**"다음은 ~다"는 글쓴이가 실제로 하려는 일만 적는다. AI가 만들어 넣지 않는다.**

예고는 장식이 아니라 **다음 글 도입부가 이어받는 약속**이다. 이 블로그는 매 글이
앞 글의 예고를 인용하며 시작한다. 그래서 지어낸 예고는 그 자리에서 끝나지 않고,
다음 글에서 "예고와 다른 길로 갔다"고 정정해야 하는 빚이 된다.
회고 글의 값은 정직함에서 나오는데, 마지막 문장이 지어낸 것이면 그 값이 깎인다.

그럴듯한 다음 단계를 **추측해서 쓰지 않는다.** 작업 맥락상 자연스러워 보여도
글쓴이의 계획이 아닐 수 있다. 실제로 2026-08-31 글에서, 앞 글의 예고 두 가지가
모두 글쓴이의 계획이 아니어서 도입부에서 "둘 다 아니다"로 시작해야 했다.

### 물어볼 것
1. **다음에 실제로 할 작업이 무엇인가** — 오늘 작업의 연장인지, 다른 것으로 넘어가는지
2. **왜 그것인가** — 예고 문장에 이유가 한 줄 붙으면 다음 글 도입부가 훨씬 쉬워진다
3. (선택) 오늘 미뤄둔 것 중 **살아 있는 것과 접은 것**

### 답을 못 받았을 때
지어내지 말고 **예고를 비운다.** "다음은 아직 정하지 않았다"거나, 오늘 남은 숙제를
사실대로 적는 편이 낫다. 없는 계획을 만드는 것보다 비어 있는 편이 정직하다.

## 출력
글 하단에 붙일 마무리 블록(표 또는 체크리스트).
**다음 예고는 글쓴이에게 물어서 받은 내용으로 쓴다.** 아직 못 받았으면 그 자리를
비워두고 무엇을 물어야 하는지 함께 제시한다.


---

<a id="readme-standard-check"></a>

# 스킬: readme-standard-check

---
name: readme-standard-check
description: 프로젝트 README.md가 "정석적인 구성"을 갖췄는지 점검하고, 실제 코드/폴더 상태와 어긋난 오래된 문구(예: "연동 예정"인데 이미 완료됨, 옛날 폴더 구조, 틀린 경로)를 찾아 사용자 승인 하에 고쳐주는 스킬. 사용자가 "README 확인해줘", "README 점검해줘", "리드미 정석인지 봐줘", "리드미 검토해줘", "이거 표준 형식 맞아?" 라고 하거나, 모두의연구소 퀘스트·개인 프로젝트를 마무리하며 제출·배포 직전에 상태 점검이 필요할 때 반드시 사용할 것. "로드맵/개발 단계가 실제 완료된 곳까지 다 반영됐는지 모르겠다", "README에 스크린샷이 없는 것 같다", "이 정도면 충분한 문서인가?" 같은 애매한 질문에도 트리거.
---

# README 정석 체크

## 왜 이 스킬이 필요한가

README는 프로젝트를 처음 보는 사람(채점자·동료·미래의 나 자신)이 제일 먼저 읽는 문서다. 그런데 실제로 자주 생기는 문제가 있다:

- 개발 초반에 쓴 문구("Supabase 연동 예정")가 나중에 실제로 구현됐는데도 그대로 방치됨
- 화면 경로가 리팩터링 후 바뀌었는데 README엔 옛날 경로가 남음
- "여기까지 완료"라고 적은 로드맵이 실제로는 그 뒤에도 많은 작업(보안점검, 문서화, 배포, 피어리뷰 등)이 끝났는데 반영이 안 됨
- 정작 화면이 어떻게 생겼는지 스크린샷이 없어서, 코드를 열어보기 전엔 아무것도 안 보임

이 스킬은 "README가 지금 실제 프로젝트 상태를 정확히·충분히 보여주고 있는가"를 점검하는 것이지, 단순히 오타를 고치는 게 아니다. **점검(읽기) → 사용자에게 보고 → 승인 → 수정** 순서를 반드시 지킨다. 저장소 파일을 사용자 동의 없이 마음대로 고치지 않는다.

## 체크리스트 (정석 README의 구성 요소)

아래 항목을 실제 README와 대조한다. 모든 항목이 모든 프로젝트에 필수는 아니다 — "과제 제출용"과 "포트폴리오/오픈소스 공개용"은 기준이 다르다 (아래 "프로젝트 성격 구분" 참고).

| # | 항목 | 설명 |
|---|------|------|
| 1 | 제목 + 한 줄 소개 | 프로젝트가 뭔지 첫 줄만 읽어도 알 수 있는가 |
| 2 | 라이브 데모/배포 링크 | 배포된 게 있다면 그 주소가 눈에 띄게 있는가 |
| 3 | 서비스 소개 | 무엇을 / 누구를 위해 / 핵심 기능이 짧게 정리돼 있는가 |
| 4 | 화면·기능 구성 | 주요 화면이나 엔드포인트가 표·목록으로 정리돼 있는가 |
| 5 | 기술 스택 | 실제로 쓴 프레임워크·언어·DB가 다 나열돼 있는가 (빠진 것 없이, 안 쓰는 것도 없이) |
| 6 | 프로젝트 구조 | 적힌 폴더·파일 트리가 **지금 실제 코드와 일치**하는가 (가장 자주 어긋나는 부분) |
| 7 | 로컬 설치·실행 방법 | 남이 그대로 따라 하면 실행되는가. 환경변수가 필요하면 그것도 안내하는가 |
| 8 | 환경변수 안내 | 변수 **이름**만 예시로 보여주는가 (실제 키·비밀번호 값은 절대 넣지 않음) |
| 9 | 진행 단계/로드맵 | 지금까지 **실제로 끝난 모든 단계**가 빠짐없이 반영됐는가 (예: 프론트→배포→백엔드연동→보안점검→문서화→피어리뷰/제출준비 등, 프로젝트가 어디까지 왔는지) |
| 10 | 상세 문서 링크 | PRD·API 명세·보안 점검 같은 문서가 저장소 안(`docs/` 등)에 있다면 README에서 링크로 연결돼 있는가. 저장소 밖에 있다고 적혀 있으면 오래된 문구일 가능성이 큼 |
| 11 | 스크린샷/데모 이미지 | 화면이 있는 서비스인데 캡처가 하나도 없는가 (있으면 좋음, 특히 웹 서비스는 관례상 거의 필수) |
| 12 | 라이선스 | 오픈소스로 공개할 프로젝트면 필요, 학교 과제면 선택사항 |
| 13 | 뱃지·목차 | 있으면 보기 좋지만 필수는 아님 (README가 길 때만 목차가 의미 있음) |

## 진행 순서

### 1단계 — README 읽기
대상 프로젝트의 README.md를 읽는다.

### 2단계 — 실제 코드와 대조하기
아래를 확인해서 README 내용이 "지금 실제로 맞는 말인지" 검증한다. 이 단계가 이 스킬의 핵심이다 — README만 읽고 판단하면 안 되고, 반드시 실제 코드를 확인해야 한다.

- `Glob`/`Grep`으로 실제 폴더·파일 구조를 뽑아, README의 "프로젝트 구조" 섹션과 다른 부분이 있는지 비교
- README가 "OO 연동 예정"이라고 했는데, 관련 코드(예: DB 클라이언트, API 호출)가 이미 존재하면 → 이미 완료된 것이므로 문구가 오래됨
- README에 적힌 화면 경로(`/foo/[id]` 등)가 실제 라우트 파일 구조와 일치하는지 확인
- `docs/`, `PRD`, `API명세`, `보안점검` 같은 문서 파일이 저장소 안에 실제로 있는지 확인 (있는데 README가 "저장소 밖에 있다"고 하면 오래된 것)
- git 저장소라면 로드맵에 적힌 마지막 단계 이후에도 커밋이 있었는지(`git log`) 봐서, 로드맵이 실제 최신 진행 상황까지 반영됐는지 확인
- 스크린샷 파일(`.png`, `.jpg`, `.gif` 등)이 저장소나 README 안에 실제로 참조돼 있는지 확인

### 3단계 — 프로젝트 성격 구분
사용자에게 (또는 맥락에서) 이 프로젝트가 **과제 제출용**인지 **포트폴리오/오픈소스 공개용**인지 확인한다. 이미 대화 맥락에서 알 수 있으면 물어보지 않아도 된다.

- 과제 제출용: 1~10번이 핵심, 11~13번(스크린샷·라이선스·뱃지)은 있으면 좋지만 없어도 크게 문제 없음
- 포트폴리오/오픈소스 공개용: 11번(스크린샷)은 사실상 필수, 12번(라이선스)도 챙기는 게 좋음

### 4단계 — 점검 결과 보고 (여기서 멈추고 사용자 확인받기)
아래 표 형식으로 정리해서 사용자에게 먼저 보여준다. **이 단계에서 아직 파일을 고치지 않는다.**

```
| 항목 | 상태 | 비고 |
|------|------|------|
| 제목/한줄소개 | ✅ 있음 | |
| 라이브 링크 | ✅ 있음 | |
| 기술 스택 | ⚠️ 오래됨 | "OO 연동 예정"이라고 되어있는데 이미 구현됨 |
| 프로젝트 구조 | ⚠️ 오래됨 | 실제론 lib/requests.ts가 생겼는데 README엔 없음 |
| 로드맵 | ❌ 없음/부족 | 배포·보안점검 이후 단계가 반영 안 됨 |
| 스크린샷 | ❌ 없음 | (과제용이라 필수는 아니지만 있으면 좋음) |
```

그리고 짧게 총평 한 줄: "과제 제출 기준으로는 [충분함/부족함], 포트폴리오 기준으로는 [스크린샷 등] 보강하면 좋겠습니다." 같은 식으로.

### 5단계 — 사용자 승인 받고 수정
사용자가 "고쳐줘" 또는 특정 항목만 골라서 승인하면, **그 프로젝트의 실제 현재 상태에 맞게** 채워 넣는다. 지어내지 말고, 2단계에서 실제로 확인한 사실만 반영한다.

수정 시 반드시 지킬 것:
- **비밀값 절대 금지**: API 키, DB 접속 문자열, 비밀번호, 토큰 등 실제 값은 README에 어떤 형태로도 넣지 않는다. 환경변수 안내는 변수 **이름**만 예시로 보여준다 (`NEXT_PUBLIC_SUPABASE_URL=your-project-url` 처럼).
- **git 저장소라면 커밋 전에**: `git fetch`로 원격 상태를 확인하고, 다른 사람/다른 세션이 같은 파일을 먼저 바꿨는지 본다. 바뀌었으면 받아서 병합한 뒤 진행하고, **강제 push는 하지 않는다.**
- 수정 후 커밋 전 **최종 비밀값 재검사**(grep으로 API 키 패턴, 토큰 패턴 등 확인)를 한 번 더 한다.
- 실제로 push까지 할지는 사용자에게 확인한다.

### 6단계 — (선택) 실제 반영 확인
push했다면, 가능하면 실제 배포된 페이지나 GitHub 렌더링 결과를 열어서 의도대로 보이는지 확인한다 (특히 체크박스·표 같은 마크다운은 텍스트로만 보면 놓치기 쉬우니, 가능하면 브라우저로 실제 렌더링을 확인).

## 말투 안내

사용자는 코딩·GitHub을 배우는 중일 수 있다. "JSON", "CI", "머지 충돌" 같은 용어를 쓸 때는 짧게 풀어서 설명하고, 표·이모지를 활용해 한눈에 보이게 정리한다. 어려운 개념을 설명할 땐 실생활 비유(식당의 홀/주방 비유 등)를 써도 좋다.


---

<a id="term-card"></a>

# 스킬: term-card

---
name: term-card
description: 새로 배운 기술 용어를 조경호 블로그의 '용어정리' 카드 형식(정의·비유·예시 3단)으로 정리할 때 사용. 여러 개를 갈래로 묶어 '오늘의 용어 정리' 글로 만들고, 전에 정리했던 용어가 다시 나오면 「다시 정리 — N번째」로 표시한다.
---

# 용어정리 카드 (term-card)

> ⚠️ **먼저 [[lms-safe]]를 적용한다.** 용어 정의·비유·예시를 LMS 원문에서 베끼지 말고, 자체 소화해 내 말로 쓴다(공인 전문용어 자체는 예외). 게시 전 lms-safe 체크리스트 통과.

배운 용어를 조경호 블로그의 고정 3단 템플릿으로 정리한다.
블로그 전 글에서 100% 일관되게 쓰는 형식이므로 **틀을 절대 바꾸지 않는다**.

## 카드 하나의 형식

```markdown
### N. 용어명(English 원어)
- **정의**: 무엇인지 한두 문장. 약어면 원어 풀이를 함께.
- **비유**: 일상에서 바로 떠오르는 비교 하나.
- **예시**: 실제로 어디에 쓰는지 / 주의점 하나.
```

카드가 10개를 넘으면 **갈래로 묶는다**(최근 글들이 쓰는 구조).
`## 갈래 N. 묶음 이름` 아래에 `### N. 용어` 카드를 넣고,
글 앞부분에 갈래별로 어떤 낱말이 들어갔는지 표로 먼저 보여준다.
카드 번호는 갈래가 바뀌어도 **글 전체에서 1부터 연속**으로 매긴다.

규칙:
- 세 줄은 항상 **정의 → 비유 → 예시** 순서, 이 순서를 바꾸지 않는다.
- 핵심 단어는 `**굵게**` 강조(정의 안의 키워드, 예시 안의 조건 등).
- 비유는 계산대·바구니·창고·메모판처럼 **손에 잡히는 사물**로. 추상적 비유 금지.
- 원어(영어)는 `*이탤릭*` 또는 괄호로 병기.
- **용어를 비유로 대체하지 않는다.** 정의는 정확한 용어로 쓰고, 비유는 그 아래 한 줄 보조다.
  용어정리 글은 3단 형식이 블로그 전체의 일관성이므로 「비유」 줄 자체는 유지하되,
  억지로 갖다 붙이지는 않는다. 마땅한 비유가 없으면 **개념 간 대비**로 대신한다
  (예: "Schema는 데이터 전체의 구조 / Argument Schema는 함수 입력값의 구조").

## 실제 예시 (블로그 원본 톤)

```markdown
### 1. 캐시(Cache)
- **정의**: 자주 쓰는 데이터를 **메모리에 잠깐** 두어 빠르게 꺼내 쓰는 것.
- **비유**: 매번 창고(디스크·DB)까지 안 가고, 자주 찾는 물건을 **계산대 옆 바구니**에 올려 둠.
- **예시**: 로그인 세션 확인. 대신 원본과 잠깐 어긋날 수 있어 **수명(TTL)** 을 정해 둔다.
```

## 이미 정리한 용어가 또 나오면 — 「다시 정리」 표시 (2026-08-27 도입)

**이전 용어정리 글에서 카드로 정리한 용어가 다시 나오면, 지우지 말고 표시해서 다시 싣는다.**
반복해서 물어본 용어는 아직 손에 안 붙은 용어라는 기록이고, 그 자체가 정보다.

카드 제목 바로 아래에 인용 블록으로 붙인다.

```markdown
### 9. Schema(스키마)
> ※ 다시 정리 — **세 번째.** 두 번 정리했는데 또 물었다. 지금 가장 약한 자리다.
- **정의**: ...
```

규칙:
- 문구는 **`※ 다시 정리 — N번째`**. "중복"이라고 쓰지 않는다 —
  「중복」은 쓸데없이 또 나왔다는 뜻으로 읽히고 글쓴이를 깎는다. 반복은 복습이다.
- **횟수를 센 기준은 「용어정리 글에서 카드로 정리한 것」만.** 다른 글 본문에서
  지나가듯 쓴 것은 정리한 게 아니므로 세지 않는다.
- 언제 정리했는지 한 조각 덧붙인다(예: "7월에 정리했다"). 세 번째 이상이면
  그게 지금 가장 약한 자리라는 걸 명시한다.
- **표시만 하고 정의는 새로 쓴다.** 이전 글 문장을 복사하지 않는다.

횟수를 세는 방법 — 기존 용어정리 글들의 카드 제목을 뽑아 대조한다.

```bash
grep -h "^### " $(grep -l "categories: \[일지, 용어정리\]" _posts/*.md)
```

글 앞부분에 기준을 한 줄 밝히고, 마무리에서 **몇 개가 두 번째·세 번째인지**와
어떤 종류의 용어가 반복되는지(예: 구조·규칙을 가리키는 말) 짚으면 글이 스스로
학습 패턴을 드러낸다.

## 여러 개를 묶을 때 (= '오늘의 용어 정리' 글)
- 제목: `"오늘의 용어 정리 — 용어1·용어2·용어3"` (가운뎃점 `·`으로 나열)
- `categories: [일지, 용어정리]`, `tags:`에 각 용어를 그대로
- 카드를 `### 1.`부터 번호순으로 나열(10개 넘으면 갈래로 묶는다)
- 이전에 정리한 용어가 섞여 있으면 위의 **「다시 정리」 표시**를 붙인다
- Front matter는 [[jekyll-frontmatter]] 규칙을 따른다

## 출력
카드(들) 마크다운. 묶음 글이면 front matter + 파일명 `YYYY-MM-DD-terms-주제.md`까지 함께.


---

<a id="tone-keeper"></a>

# 스킬: tone-keeper

---
name: tone-keeper
description: 블로그 글 초안을 조경호 특유의 말투(대화체·성찰형·'알고 가기')로 교정할 때 사용. 과장·홍보체를 걷어내고 과정과 이해 중심의 담담한 톤으로 통일한다.
---

# 말투 교정 (tone-keeper)

이미 쓴 초안을 받아 **내용은 유지하고 톤만** 조경호 블로그 목소리로 맞춘다.

## 조경호 톤의 특징
- **대화체·성찰형.** 독자에게 말 걸듯, 스스로 돌아보듯.
- **속도보다 이해.** "빨리 끝냈다"가 아니라 "왜 이렇게 되는지 알고 갔다".
- **정직함.** "실수도 했다", "여기서 막혔다" 같은 취약함을 숨기지 않는다.
- **정식 용어 먼저, 비유는 예외.** 용어를 쉬운 말로 **바꿔치지 않는다.**
  이해가 실제로 걸리는 개념에만, 처음 나올 때 한 줄 보조로 붙인다(없어도 된다) → [[analogy-maker]].
- **은유의 일관성.** 그 글의 은유(문·상자·기억)를 끝까지 끌고 간다.
- 자주 쓰는 표현: "알고 가기", "~한 날", "다음은 ~다".

## 걷어낼 것 (안티패턴)
- 홍보·자랑체: "완벽하게", "손쉽게", "혁신적으로" → 담담하게.
- 근거 없는 단정 → "직접 확인해보니 ~였다"로.
- 과한 감탄사·이모지 남발 → 절제.
- 정식 용어를 쉬운 말로 **대체**하기 — 「PR」을 「검토 요청」, 「머지」를 「합치기」로
  바꾸면 글쓴이가 그 말을 못 쓰는 것처럼 읽힌다. 용어는 그대로 쓰고 설명을 덧붙인다.
- 전문 용어를 **다른 전문 용어로만** 설명해서 이해가 막히는 곳 → 그 자리엔 비유 한 줄(정말 막힐 때만).

## 교정 방식
1. 문장 의미는 그대로 두고 어조만 바꾼다.
2. 자랑/과장 표현을 과정 서술로 치환한다.
3. 용어는 **그대로 둔다.** 이해가 실제로 걸리는 대목에만 비유 한 줄을 보조로 붙인다(대부분은 안 붙인다).
   아래 「이미 익숙한 어휘」는 풀어쓰지 않는다.
4. 은유가 중간에 끊기면 이어 붙인다.
5. 마지막 줄이 다음 글로 이어지는 여운("다음은 ~")으로 끝나는지 확인.

## 출력
교정된 전문. 크게 바꾼 부분은 무엇을·왜 바꿨는지 짧게 덧붙인다.


## 이미 익숙한 어휘 — 풀어쓰지 말 것

**블로그에 4편 이상에서 직접 쓴 말은 정확히 이해하고 쓰는 어휘다.**
쉬운 말로 바꿔치면 글쓴이가 그 말을 못 쓰는 것처럼 읽힌다. 그대로 쓴다.

| 어휘 | 쓴 글 | 어휘 | 쓴 글 |
|---|---|---|---|
| 모델 | 28편 | 도커 · 컨테이너 · 이미지 | 5~10편 |
| 로그 | 31편 | 임베딩 | 4편 |
| 검증 | 27편 | 환경변수 | 8편 |
| 에이전트 | 16편 | 프롬프트 | 7편 |
| 배포 · 빌드 | 5~17편 | 포트 | 8편 |
| 세션 · 컨텍스트 | 4~14편 | 캐시 | 7편 |
| 커밋 · 브랜치 · 머지 · PR | 4~14편 | 시드 | 4편 |
| 토큰 | 17편 | 워크플로 · 파이프라인 | 4편 |

*2026-08-21 기준 · 글 51편에서 추출*

**판단 기준은 목록이 아니라 「본인이 쓰는 말인가」다.** 목록에 없어도 대화에서
이미 쓰고 있는 말이면 같은 규칙을 적용한다. 반대로 목록에 있어도 그 글에서 처음
등장하는 개념이면 「알고 가기」 한 줄은 붙인다 — **대체가 아니라 보강.**

**갱신** — 분기마다 다시 뽑는다.

```bash
grep -rl "<용어>" _posts | wc -l
```

